services:
  ZobiTools:
    build: .
    container_name: ZobiTools
    restart: always
    env_file: .env
    ports:       
      - "3000" 
    networks:
      - global

networks:
  global:
    external: true
